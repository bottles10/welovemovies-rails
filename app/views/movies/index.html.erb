<div class="py-10">
    <div>
        <h2 class="mb-6 p-3 text-4xl border-b">All Movies</h2>
        <% if @movies.any? %>
            <%= render @movies %>
        <% else %>
            <p>There are no movies!</p>
        <% end %>
    </div>
</div>